# Test and lint tooling. The unit suite needs no network and no credentials.
pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.12
pytest-timeout>=2.2
ruff>=0.6
# Imported by the dashboard-builder test to render card templates.
jinja2>=3.1
