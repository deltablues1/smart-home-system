# Runtime dependencies, pinned to the versions running on the Raspberry Pi 5
# (Python 3.11, Raspberry Pi OS, aarch64). Every package here is imported by
# the code in this repository; nothing is listed "just in case".

# --- Agents and models -------------------------------------------------------
google-adk==1.31.1
google-genai==1.73.1
# Claude through ADK's LiteLlm wrapper (LLM_PROVIDER=anthropic).
litellm==1.89.1
# OpenAI TTS (cedar voice) and the optional OpenAI STT engine.
openai==2.42.0

# --- Google Workspace and Cloud ---------------------------------------------
google-auth==2.49.2
google-auth-oauthlib==1.3.1
google-auth-httplib2==0.3.1
google-api-python-client==2.194.0
google-cloud-firestore==2.27.0
google-cloud-logging==3.15.0
# Vertex AI RAG corpora (Socrates, Christian guide) and Imagen/Veo helpers.
google-cloud-aiplatform==1.148.1
google-cloud-storage==3.10.1

# --- Web API, Telegram, scheduling ------------------------------------------
fastapi==0.136.0
uvicorn[standard]==0.45.0
sse-starlette==3.3.4
python-telegram-bot==22.7
APScheduler==3.11.2
aiohttp==3.13.5
httpx==0.28.1
requests==2.33.1
pydantic==2.13.3
python-dotenv==1.2.2
pytz==2026.1.post1

# --- Smart home --------------------------------------------------------------
paho-mqtt==2.1.0
# Home Assistant WebSocket API (long-term statistics, dashboards).
websockets==15.0.1
# Home Assistant Assist <-> Jarvis speech bridge.
wyoming==1.10.0

# --- Research and documents -------------------------------------------------
beautifulsoup4==4.14.3
firecrawl-py==4.38.0
Markdown==3.10.2
pypdf==6.10.2
pillow==12.2.0
youtube-transcript-api

# --- Wake word and audio (Pi only) -------------------------------------------
# openWakeWord is free and offline. Porcupine is an optional alternative:
# set WAKEWORD_ENGINE=porcupine and `pip install pvporcupine`.
openwakeword==0.6.0
onnxruntime==1.25.0
sounddevice==0.5.5
numpy==2.4.4
